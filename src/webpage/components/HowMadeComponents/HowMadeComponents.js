import React from 'react';
import {VideoPlayer} from '..';

const HowConnectionClickShare = (props) => {
    return(<VideoPlayer poster={props.poster} video={props.video}/>)
}

export default HowConnectionClickShare